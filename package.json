{
    "name": "rcmg-stream-server",
    "version": "1.0.0",
    "scripts": {
        "start": "node server.js",
        "build": "apt-get update && apt-get install -y ffmpeg && npm install"
    },
    "dependencies": {
        "dotenv": "^16.4.1",
        "express": "^4.18.2",
        "cors": "^2.8.5",
        "child_process": "^1.0.2"
    },
    "engines": {
        "node": "18.x"
    }
}